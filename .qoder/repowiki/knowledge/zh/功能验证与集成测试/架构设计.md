- 采用扁平化脚本结构，每个文件针对特定技术栈或业务流进行独立冒烟测试。
- 深度耦合 `app` 模块，直接引用 `app.core.logger` 和 `app.import_process.agent` 以验证核心组件的运行时行为。
- 依赖 `python-dotenv` 处理环境变量优先级，利用 `torch` 验证硬件加速支持，并通过 `kb_import_app.stream` 执行端到端的图工作流调试。