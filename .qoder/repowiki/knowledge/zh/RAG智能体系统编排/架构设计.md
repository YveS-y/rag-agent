- **基础设施容器化**：利用 `docker-compose.yml` 声明式定义 Milvus（含内置 etcd/MinIO）、业务 MinIO 及 MongoDB 服务，并通过 `volumes/` 目录实现数据持久化。
- **应用服务解耦**：采用双 FastAPI 服务架构，将文档导入处理（端口 8000）与 RAG 查询交互（端口 8001）物理隔离，避免资源竞争。
- **统一运维脚本**：通过 `scripts/start.sh` 和 `stop.sh` 封装底层 Docker 命令与 Python 进程管理，提供一键式的环境初始化与服务生命周期控制。