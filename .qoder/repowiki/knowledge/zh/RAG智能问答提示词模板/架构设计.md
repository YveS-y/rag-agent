- 采用扁平化文件组织，每个`.prompt`文件对应一个独立的LLM交互阶段。
- 核心流程覆盖：查询理解与重写（`rewritten_query_and_itemnames.prompt`）、实体/商品提取（`item_name_recognition.prompt`）、假设性文档生成（`hyde_prompt.prompt`）以及基于上下文的最终回答合成（`answer_out.prompt`）。
- 模板统一使用 `{variable}` 占位符进行动态内容注入，并严格规定输出格式（如JSON或特定区块标记）。